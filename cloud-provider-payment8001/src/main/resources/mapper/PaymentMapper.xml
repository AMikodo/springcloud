<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.start.springcloud.dao.PaymentDao">
    <insert id="add" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment (serial) values (#{serial});
    </insert>

    <resultMap id="BaseResultMap" type="com.start.springcloud.entities.Payment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="serial" property="serial" jdbcType="VARCHAR" javaType="String"/>
        <result column="test" property="test" jdbcType="VARCHAR" javaType="String"/>
    </resultMap>


    <select id="getPaymentById" parameterType="Long" resultMap="BaseResultMap">
        select * from payment where id = #{id};
    </select>

    <select id="getPaymentById2" parameterType="Long" resultType="com.start.springcloud.entities.Payment">
        select id , serial, is_open from payment where id = #{id};
    </select>
</mapper>